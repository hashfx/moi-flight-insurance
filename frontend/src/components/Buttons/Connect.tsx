import React from "react";

const Connect = () => {
	return (
		<button className="w-button p-button bg-Primary border border-Secondary rounded-lg">
			Connect
		</button>
	);
};

export default Connect;
